{
  "name": "decoupled-data-markdown",
  "version": "1.0.0",
  "description": "Markdown with YAML parser for Decoupled",
  "main": "index.js",
  "types": "index.d.ts",
  "engines": {
    "node": "node >= 8.0.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "../../scripts/build-package.sh",
    "watch": "tsc --watch",
    "lint": "tslint -t verbose src/**/*.ts{,x}",
    "test": "jest --config jest.config.js"
  },
  "files": [
    "/lib",
    "/index.*"
  ],
  "dependencies": {
    "jest": "^24.9.0",
    "showdown": "^1.9.0",
    "typescript": "^3.6.*",
    "yaml": "^1.7.0"
  },
  "devDependencies": {
    "@types/yaml": "^1.0.2",
    "@types/jest": "^24.0.*",
    "@types/node": "^12.7.*"
  }
}
