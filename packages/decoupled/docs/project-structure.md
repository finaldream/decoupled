# Project Structure

```
 - root
  |
  +- sites
    |
    +- assets (optional) - custom frontend assets folder
    +- config (required) - site's configuration, inherits from `default`-site
    +- static (optional) - static files
    +- views (optional) - server-side view components (canfigurable)
```
